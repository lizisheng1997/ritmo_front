export enum userLevelEnums {
  普通成员 = 0,
  行星会员,
  恒星会员,
  机构会员
}

export enum userLevelEnEnums {
  'Ordinary member' = 0,
  'Planet',
  'Star',
  'Member institution'
}

export enum spaceLevelEnums {
  初级 = 1,
  高级,
}

export enum reservationsEnums {
  待支付 = 0,
  待使用,
  使用中,
  已完成,
  已取消,
}

export enum reservationsColorEnums {
  '#FF3434' = 0,
  '#232322',
  '#898784',
  '#898782',
  '#898781',
}


export enum ordersEnums {
  待支付 = 0,
  已支付,
  已取消,
}